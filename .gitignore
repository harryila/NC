# pinned external dependency (brought in by setup.sh, not tracked)
external/

# data + run artifacts
data/

runs/
wandb/
*.pth
*.ckpt
*.pt

# large / derived data artifacts (pulled from GPU box — never commit regardless of location)
*.tfrecords
*.tfrecord
*.npy
*.npz

# python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

.DS_Store

# local ssh / git-auth setup notes (host, PAT steps) — never commit
cursor_ssh_configuration_for_neuralcomb.md

# private outreach contacts (researcher names/notes) — never commit
OUTREACH-stanford-berkeley.md
